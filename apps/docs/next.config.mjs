/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: [
    '@hilal-ds/core',
    '@hilal-ds/icons',
    '@hilal-ds/patterns',
    '@hilal-ds/react',
    '@hilal-ds/tokens',
  ],
};

export default nextConfig;
